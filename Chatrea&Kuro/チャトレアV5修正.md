V5 の方向性はかなり良いです。
特に
- discrete burst を最優先にしたこと
- wav 長基準で step を考え直したこと
- 2モード + reset に整理したこと

この3点は採用寄りです。

1点だけ修正したいです。
base_length の切替に使っている gswitch2 は、
「2入力から1つを選んで1出力にまとめる」用途とは噛み合いません。
gswitch2 は右inletの入力をどちらのoutletへ出すかを切り替える object です。
今回の base_length 切替は Max message / number の2入力→1出力なので、
switch など、入力選択型の object で組み直したいです。

加えて、wav 全体長の取得は info~ を第一候補にしてよさそうです。
line~ envelope 案も方向としては良いので、
次は
1. base_length 切替 object の修正
2. info~ で length 取得
3. line~ / *~ の burst 層を最小実装
の順で絞るのが良いと思います。
